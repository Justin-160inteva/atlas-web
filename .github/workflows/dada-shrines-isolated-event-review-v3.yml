name: Dada Shrine Isolated Event Review v3 (Deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: |
          echo 'This workflow validated the payload-safe review but used a non-atomic branch publish step.'
          echo 'Use Dada Shrine Isolated Event Review v4, which persists a non-pixel artifact and publishes from the latest branch state.'
