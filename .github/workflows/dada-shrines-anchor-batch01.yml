name: Dada Shrine Anchor Batch 01 (Deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: |
          echo 'This equal-duration shrine workflow is deprecated.'
          echo 'Use Dada Shrine Scene Segmentation, which detects visual boundaries and reviews isolated high-resolution event batches.'
