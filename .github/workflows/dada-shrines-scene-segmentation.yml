name: Dada Shrine Scene Segmentation (Deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: |
          echo 'This workflow used oversized multi-frame event requests and is deprecated.'
          echo 'Use Dada Shrine Isolated Event Review v3, which sends one representative frame per explicitly labeled event.'
